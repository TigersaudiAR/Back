# Local Recitation Assets

Actual audio is generated from base64 placeholders stored under `src/data/quran/recitations` to keep the repository free of binary blobs.
The runtime hook still exposes offline `data:` URLs so the application behaves identically without network requests.
